/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  string,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  Participant,
  ParticipantArgs,
  getParticipantSerializer,
} from '../types';

export type SplitterConfig = Account<SplitterConfigAccountData>;

export type SplitterConfigAccountData = {
  discriminator: Uint8Array;
  authority: PublicKey;
  name: string;
  participants: Array<Participant>;
  botWallet: PublicKey;
  incentiveBps: number;
  bump: number;
};

export type SplitterConfigAccountDataArgs = {
  authority: PublicKey;
  name: string;
  participants: Array<ParticipantArgs>;
  botWallet: PublicKey;
  incentiveBps: number;
  bump: number;
};

export function getSplitterConfigAccountDataSerializer(): Serializer<
  SplitterConfigAccountDataArgs,
  SplitterConfigAccountData
> {
  return mapSerializer<
    SplitterConfigAccountDataArgs,
    any,
    SplitterConfigAccountData
  >(
    struct<SplitterConfigAccountData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['authority', publicKeySerializer()],
        ['name', string()],
        ['participants', array(getParticipantSerializer(), { size: 5 })],
        ['botWallet', publicKeySerializer()],
        ['incentiveBps', u8()],
        ['bump', u8()],
      ],
      { description: 'SplitterConfigAccountData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([37, 70, 66, 89, 147, 213, 59, 153]),
    })
  ) as Serializer<SplitterConfigAccountDataArgs, SplitterConfigAccountData>;
}

export function deserializeSplitterConfig(
  rawAccount: RpcAccount
): SplitterConfig {
  return deserializeAccount(
    rawAccount,
    getSplitterConfigAccountDataSerializer()
  );
}

export async function fetchSplitterConfig(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<SplitterConfig> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'SplitterConfig');
  return deserializeSplitterConfig(maybeAccount);
}

export async function safeFetchSplitterConfig(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<SplitterConfig | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeSplitterConfig(maybeAccount) : null;
}

export async function fetchAllSplitterConfig(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<SplitterConfig[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'SplitterConfig');
    return deserializeSplitterConfig(maybeAccount);
  });
}

export async function safeFetchAllSplitterConfig(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<SplitterConfig[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeSplitterConfig(maybeAccount as RpcAccount)
    );
}

export function getSplitterConfigGpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'splits',
    'FM9hKTFN98M2uo7zw2huAbx7vJTQpfgFuxr9rVCTt8UY'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      discriminator: Uint8Array;
      authority: PublicKey;
      name: string;
      participants: Array<ParticipantArgs>;
      botWallet: PublicKey;
      incentiveBps: number;
      bump: number;
    }>({
      discriminator: [0, bytes({ size: 8 })],
      authority: [8, publicKeySerializer()],
      name: [40, string()],
      participants: [null, array(getParticipantSerializer(), { size: 5 })],
      botWallet: [null, publicKeySerializer()],
      incentiveBps: [null, u8()],
      bump: [null, u8()],
    })
    .deserializeUsing<SplitterConfig>((account) =>
      deserializeSplitterConfig(account)
    )
    .whereField(
      'discriminator',
      new Uint8Array([37, 70, 66, 89, 147, 213, 59, 153])
    );
}
