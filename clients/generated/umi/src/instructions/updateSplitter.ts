/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  string,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  Participant,
  ParticipantArgs,
  getParticipantSerializer,
} from '../types';

// Accounts.
export type UpdateSplitterInstructionAccounts = {
  authority: Signer;
  splitterConfig: PublicKey | Pda;
};

// Data.
export type UpdateSplitterInstructionData = {
  discriminator: Uint8Array;
  name: string;
  participants: Array<Participant>;
  botWallet: PublicKey;
};

export type UpdateSplitterInstructionDataArgs = {
  name: string;
  participants: Array<ParticipantArgs>;
  botWallet: PublicKey;
};

export function getUpdateSplitterInstructionDataSerializer(): Serializer<
  UpdateSplitterInstructionDataArgs,
  UpdateSplitterInstructionData
> {
  return mapSerializer<
    UpdateSplitterInstructionDataArgs,
    any,
    UpdateSplitterInstructionData
  >(
    struct<UpdateSplitterInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['name', string()],
        ['participants', array(getParticipantSerializer(), { size: 5 })],
        ['botWallet', publicKeySerializer()],
      ],
      { description: 'UpdateSplitterInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([45, 2, 199, 7, 96, 249, 137, 255]),
    })
  ) as Serializer<
    UpdateSplitterInstructionDataArgs,
    UpdateSplitterInstructionData
  >;
}

// Args.
export type UpdateSplitterInstructionArgs = UpdateSplitterInstructionDataArgs;

// Instruction.
export function updateSplitter(
  context: Pick<Context, 'programs'>,
  input: UpdateSplitterInstructionAccounts & UpdateSplitterInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'splits',
    'FM9hKTFN98M2uo7zw2huAbx7vJTQpfgFuxr9rVCTt8UY'
  );

  // Accounts.
  const resolvedAccounts = {
    authority: {
      index: 0,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    splitterConfig: {
      index: 1,
      isWritable: true as boolean,
      value: input.splitterConfig ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateSplitterInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdateSplitterInstructionDataSerializer().serialize(
    resolvedArgs as UpdateSplitterInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
