/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { SPLITS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getParticipantDecoder,
  getParticipantEncoder,
  type Participant,
  type ParticipantArgs,
} from '../types';

export const UPDATE_SPLITTER_DISCRIMINATOR = new Uint8Array([
  45, 2, 199, 7, 96, 249, 137, 255,
]);

export function getUpdateSplitterDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_SPLITTER_DISCRIMINATOR
  );
}

export type UpdateSplitterInstruction<
  TProgram extends string = typeof SPLITS_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSplitterConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSplitterConfig extends string
        ? WritableAccount<TAccountSplitterConfig>
        : TAccountSplitterConfig,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateSplitterInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
  participants: Array<Participant>;
  botWallet: Address;
};

export type UpdateSplitterInstructionDataArgs = {
  name: string;
  participants: Array<ParticipantArgs>;
  botWallet: Address;
};

export function getUpdateSplitterInstructionDataEncoder(): Encoder<UpdateSplitterInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['name', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['participants', getArrayEncoder(getParticipantEncoder(), { size: 5 })],
      ['botWallet', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_SPLITTER_DISCRIMINATOR })
  );
}

export function getUpdateSplitterInstructionDataDecoder(): Decoder<UpdateSplitterInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['name', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['participants', getArrayDecoder(getParticipantDecoder(), { size: 5 })],
    ['botWallet', getAddressDecoder()],
  ]);
}

export function getUpdateSplitterInstructionDataCodec(): Codec<
  UpdateSplitterInstructionDataArgs,
  UpdateSplitterInstructionData
> {
  return combineCodec(
    getUpdateSplitterInstructionDataEncoder(),
    getUpdateSplitterInstructionDataDecoder()
  );
}

export type UpdateSplitterInput<
  TAccountAuthority extends string = string,
  TAccountSplitterConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  splitterConfig: Address<TAccountSplitterConfig>;
  name: UpdateSplitterInstructionDataArgs['name'];
  participants: UpdateSplitterInstructionDataArgs['participants'];
  botWallet: UpdateSplitterInstructionDataArgs['botWallet'];
};

export function getUpdateSplitterInstruction<
  TAccountAuthority extends string,
  TAccountSplitterConfig extends string,
  TProgramAddress extends Address = typeof SPLITS_PROGRAM_ADDRESS,
>(
  input: UpdateSplitterInput<TAccountAuthority, TAccountSplitterConfig>,
  config?: { programAddress?: TProgramAddress }
): UpdateSplitterInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountSplitterConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? SPLITS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    splitterConfig: { value: input.splitterConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.splitterConfig),
    ],
    programAddress,
    data: getUpdateSplitterInstructionDataEncoder().encode(
      args as UpdateSplitterInstructionDataArgs
    ),
  } as UpdateSplitterInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSplitterConfig
  >;

  return instruction;
}

export type ParsedUpdateSplitterInstruction<
  TProgram extends string = typeof SPLITS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    splitterConfig: TAccountMetas[1];
  };
  data: UpdateSplitterInstructionData;
};

export function parseUpdateSplitterInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateSplitterInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      splitterConfig: getNextAccount(),
    },
    data: getUpdateSplitterInstructionDataDecoder().decode(instruction.data),
  };
}
